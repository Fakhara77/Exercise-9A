//Q20:Use some to check if some names' length greater than seven in names array
const names = ['Asabeneh', 'Mathias', 'Elias', 'Brook'];
const newName = names.some((name) => name.length > 7);
console.log(newName);