//// Q:1// higheer oder function
      //for eachloop:we use forEach with arrays.it iterate array element.it take two callback function,element and index,and array itself .but   the array and index is opitional.
      //map:iterae  the array element and modify the array element. it take callback function and element,index and parameter and return a new array;
      //filter:filter out the items,which fullfill filtering condition and return a new array.\
      //reduce method: reduce  take callback function.the callback take two callback accummalator and current and opitional initional value and return a single value



//Q2:   what is callback function?
//call back is a funition which can be passed as a parameter to the other funcion